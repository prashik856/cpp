# Leetcode subrepo

This repo contains the code which I wrote to solve the leetcode problems. The solution might be pure shit and I don't guarantee that it will work.

## Compile code locally
```bash
# Compile using below commands
g++ -std=c++11 filename.cpp -o filename.out

# Run the file
./filenamme.out
```